version: "3.2"
services: 
  kmagda:
     #build: .  
     image: csiroenvinf/magda-builder  
     volumes:
        - type: bind
          source: /var/run/docker.sock
          target: /var/run/docker.sock
        - type: bind
          source: .
          target: /app
     command: tail -f /dev/null
